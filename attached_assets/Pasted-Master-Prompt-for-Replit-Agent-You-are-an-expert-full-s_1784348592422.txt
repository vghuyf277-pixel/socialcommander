Master Prompt for Replit Agent:You are an expert full-stack developer and DevOps engineer. Build a complete, production-ready, self-hosted social media management webapp in this Replit project with the following exact requirements. Use modern 2026 best practices, clean code, TypeScript where possible, and make it highly modular and well-documented so multiple AI agents can extend it seamlessly.Core Goal: A dashboard to manage 10 X (Twitter) accounts + 10 Reddit accounts. Focus on automated/scheduled posting with AI optimization for engagement, while minimizing manual input. Support per-account isolation for safety. Include open-source-inspired features from tools like Postiz or Mixpost.Tech Stack:Frontend: Next.js 15 (App Router) + TypeScript + Tailwind + Shadcn/UI + Recharts for analytics.
Backend: FastAPI (Python) or NestJS (if Node preferred) with TypeScript.
Database: PostgreSQL (use Replit DB or Postgres extension if available; include Drizzle or Prisma schema).
Queue/Scheduler: Redis + BullMQ or Celery for background jobs.
Browser Automation: Playwright with stealth plugin for posting where APIs are insufficient.
AI: Integrate Groq or OpenAI-compatible API for content generation, voice matching, best-time prediction, and A/B variants.
Auth: NextAuth or JWT, with per-account OAuth storage.

Key Features:Multi-account management: Color-coded accounts, per-account proxies, voice profiles (past post examples for AI), status monitoring (active/suspended).
Unified content calendar showing all accounts.
Post composer: Text, media, threads, platform-specific previews. AI generation with "vibe" matching per account.
Scheduling & Automation: Evergreen queues, recurring posts, jitter/randomization for human-like behavior. AI-optimized posting times based on historical engagement.
Engagement Optimization: Rules engine for auto-replies/likes on thresholds (with approval toggle). Track impressions, likes, comments.
Isolation & Safety: Per-account residential proxy support (config via env/DB). Separate Playwright contexts. Rate limiting per account/platform. Human approval workflow option.
Analytics Dashboard: Per-account and aggregate metrics, performance heatmaps.
Monitoring & Alerts: Account health, post success/fail logs, ban risk warnings.

Additional Requirements:Full error handling, retries, and logging.
Compliance: Built-in action logging, content filters, TOS warning notes in UI/docs.
Responsive dark-mode UI with easy account switching.
Environment variables for all secrets (GitHub token, AI key, proxies, OAuth).

GitHub Integration Script:
Include a complete, secure script (e.g., scripts/push-to-github.ts or Python equivalent) that:Checks for GITHUB_TOKEN in Replit Secrets.
If present, initializes git, adds remote (use repo name from env or input), commits all files (with .gitignore excluding secrets), and pushes to GitHub via GitHub API/rest (create repo if it doesn't exist, handle branches).
Provide a one-command runner (e.g., npm run push-to-github).
Make it idempotent and logged. Include instructions in README on setting the secret and running it.

Project Structure & Documentation:
Create a professional monorepo structure with:/frontend, /backend, /workers, /scripts, /docs, /config.
Comprehensive README.md with architecture overview (Mermaid diagram), setup steps, deployment guides (Replit, Railway, VPS), proxy setup, AI config, risk warnings (platform bans, TOS), and extension guide for other agents.
ARCHITECTURE.md, RISKS.md (ban risks, legal notes), TODO.md.
Inline comments and clear module boundaries.

Make It Extensible:API endpoints for external agents.
Config-driven features.
Tests stubs.
Docker Compose file for easy migration to VPS/Railway.

Security & Best Practices:Never commit secrets.
Input validation, rate limiting.
Proxy rotation/fingerprint variation.
Start with 1-2 account testing flows.

Generate the full project structure, all key files (with code), .env.example, gitignore, and the GitHub push script. After generating, summarize next steps for testing and deploying. Prioritize reliability, minimal manual input via AI automation, and clear documentation so this can be handed off to other agents or deployed elsewhere.

